---
"agent-plugins": minor
---

Enforce Conventional Commits and DCO sign-off via commitlint, run through a Husky `commit-msg` hook locally and a `commitlint` workflow on pull requests. Add the Changesets configuration (restricted access, GitHub changelog) and document the commit-message, sign-off, and changeset workflow in `CONTRIBUTING.md`.
